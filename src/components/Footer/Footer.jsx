import React from 'react';

const Footer = () => {
    return (
        <div className="p-2 bg-dark text-white text-center">
            @express and react setup
        </div>
    );
};

export default Footer;